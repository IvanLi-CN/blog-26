# 0005 Archived Note

This legacy plan covered an older development-service layout.

Current repository truth:

- `bun run dev` starts Astro, the admin SPA, and the Bun gateway
- development guidance no longer includes a separate remote content-source helper
- use current `README.md` and `AGENTS.md` instead of this archive
