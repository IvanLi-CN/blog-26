# 0002 Archived Note

This legacy plan recorded the move to persisted relative asset paths.

Current repository truth:

- persisted Markdown and metadata use relative asset paths
- runtime file reads resolve through `/api/files/local/...`
- the repository no longer carries remote content-source compatibility

Keep this directory only as archive context; do not use it as an active implementation guide.
