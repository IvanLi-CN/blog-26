# Implementation

- Lifecycle: active
- Implementation: implemented

Memo root helpers preserve the canonical `Memos` case, reject dot segments, keep inactive-source overrides from blocking startup, and align editor/API behavior with local filesystem writes.
