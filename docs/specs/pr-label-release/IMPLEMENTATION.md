# Implementation

- Lifecycle: active
- Implementation: in progress

Release intent, component-aware versioning, Pages/backend/image publishing, unified runtime packaging, and managed PR receipts are implemented through the label-driven release workflow. The release source is the exact current `main` head, which is checked before release intent can create any output. GitHub-side required-check configuration remains an operational prerequisite.
