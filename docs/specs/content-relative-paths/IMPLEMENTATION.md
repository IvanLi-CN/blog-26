# Implementation

- Lifecycle: archived
- Implementation: implemented

Relative-path persistence is established repository behavior. Local runtime reads resolve through the current file APIs, and public media delivery is owned by the public media assets facade.
