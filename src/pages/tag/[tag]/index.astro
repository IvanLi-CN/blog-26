---
import BlogList from "~/components/blog/List.astro";
import Pagination from "~/components/blog/Pagination.astro";
import Headline from "~/components/ui/Headline.astro";
import Layout from "~/layouts/PageLayout.astro";
import { blogPostsPerPage, blogTagRobots, fetchPosts } from "~/utils/blog";

export const prerender = false;

// 获取标签参数
const tagSlug = Astro.params.tag;

// 从 Astro.locals 获取管理员状态
const isUserAdmin = Astro.locals.isAdmin || false;

// 获取所有文章并按标签过滤
const allPosts = await fetchPosts(isUserAdmin);
const tagPosts = allPosts.filter((post) =>
  post.tags?.some((tag) => tag.slug === tagSlug),
);

if (tagPosts.length === 0) {
  return Astro.redirect("/404");
}

// 获取标签信息
const tag = tagPosts[0].tags?.find((t) => t.slug === tagSlug)!;

// 计算分页
const postsPerPage = blogPostsPerPage || 10;
const totalPosts = tagPosts.length;
const totalPages = Math.ceil(totalPosts / postsPerPage);
const posts = tagPosts.slice(0, postsPerPage);

// 构建分页对象
const page = {
  data: posts,
  start: 0,
  end: Math.min(postsPerPage - 1, totalPosts - 1),
  size: postsPerPage,
  total: totalPosts,
  currentPage: 1,
  lastPage: totalPages,
  url: {
    current: Astro.url.pathname,
    prev: undefined,
    next: totalPages > 1 ? `/tag/${tagSlug}/2` : undefined,
  },
};

const metadata = {
  title: `标签：${tag.title}`,
  description: `所有标记为“${tag.title}”的文章。`,
  keywords: `Ivan, Blog, 标签, ${tag.title}`,
  robots: {
    index: blogTagRobots?.index,
    follow: blogTagRobots?.follow,
  },
};
---

<Layout metadata={metadata}>
  <section class="px-4 md:px-6 py-12 sm:py-16 lg:py-20 mx-auto max-w-4xl">
    <Headline>标签：{tag.title}</Headline>
    <BlogList posts={page.data} />
    <Pagination prevUrl={page.url.prev} nextUrl={page.url.next} />
  </section>
</Layout>
