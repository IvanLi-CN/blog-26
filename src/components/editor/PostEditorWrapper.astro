---
import { PostEditorProvider } from './PostEditorProvider';

interface Props {
  postId?: string;
  isNewPost: boolean;
}

const { postId, isNewPost } = Astro.props;
---

<PostEditorProvider postId={postId} isNewPost={isNewPost} client:load />
