---
type: "agent_requested"
description: "提交代码"
---
使用 Conventional commit 风格的提交消息。
不得使用 `--no-verify` 跳过检查，所有错误都应该被解决。
提交后需要再次检查工作区是否有遗漏的提交，如果存在未提交的内容，需要向用户报告原因。
提交 (commit) 代码后，除非用于明确要求，否则不可以推送 (push)。
